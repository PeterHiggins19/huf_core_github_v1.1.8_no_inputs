# Default: normalize text files and store with LF in the repo (Windows users still edit fine).
* text=auto eol=lf

# Large binary scientific datasets (do NOT commit these to the repo; use fetch scripts)
*.fits -text

# If you ever choose to use Git LFS later, uncomment:
# *.fits filter=lfs diff=lfs merge=lfs -text
